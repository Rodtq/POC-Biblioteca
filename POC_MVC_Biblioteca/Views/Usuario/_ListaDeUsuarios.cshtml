@model POC_MVC_Biblioteca.ViewModels.AllUsersViewModel

<table class="table">
  <tr>
    <th></th>
  </tr>

  @foreach (var item in Model.UserList)
  {
      <tr>
        <td>
          <p>@Html.DisplayFor(d => item.Nome)</p>
        </td>
      </tr>
  }

</table>

